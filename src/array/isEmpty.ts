/**
 * Checks whether the given array is empty
 * @param array The array to check
 * @returns Whether it is emptry
 */
const isEmpty = (array: any[]): boolean => array.length === 0;

export default isEmpty;
